#include <stdio.h>

int main(){
	double uin, r1, r2, uout;
	printf("Enter input voltage: ");
	scanf("%lf", &uin);
	printf("Enter R1: ");
	scanf("%lf", &r1);
	printf("Enter R2: ");
	scanf("%lf", &r2);
	uout = uin * r2 / (r1 + r2);
	printf("Uout = %.3lf V\n", uout);
	return 0;
}